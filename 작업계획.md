# WaveScan — React(Vite) 전환 작업계획서

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | WaveScan |
| 작업 목적 | 퍼블리싱된 HTML/CSS/JS → React(Vite) + TypeScript 전환 |
| 원본 위치 | `org/` (기존 소스를 org 디렉토리로 이동 후 보존) |
| 프레임워크 | React 18 + Vite |
| 언어 | TypeScript |
| 스타일링 | styled-components |
| 라우터 | React Router v6 (`/` 기준) |
| 컴포넌트 위치 | `src/components/` |
| 페이지 위치 | `src/pages/` |
| 프로젝트 경로 | `D:\조윤상\WaveScan-main` |

---

## 2. 원본 소스 현황 분석

### 2-1. 페이지 목록 (HTML 파일 15개)

| HTML 파일 | CSS 파일 | JS 파일 | 설명 | 헤더/푸터 |
|-----------|---------|---------|------|-----------|
| `splash.html` | `splash.css` | `splash.js` | 스플래시/로딩 화면 | 없음 |
| `auth-start.html` | `auth-start.css` | `auth-start.js` | 시작 인증 (Google/Guest) | 없음 |
| `login.html` | `auth-start.css` | `login.js`, `auth.js` | 로그인 (auth-start와 동일 구조) | 없음 |
| `signup.html` | `signup.css` | `signup.js` | 회원가입 | 없음 |
| `index.html` | `home.css` | `home.js`, `data.js`, `auth.js` | 홈 (최근스캔, 통계, 가이드미리보기) | A형 헤더 + 하단네비 |
| `scan.html` | `scan.css` | `scan.js`, `data.js`, `auth.js` | 카메라 스캔 화면 | 없음 (전체화면) |
| `scan-result.html` | `scan-result.css` | `scan-result.js`, `data.js`, `auth.js` | 스캔 결과 | A형 헤더 + 하단네비 |
| `history.html` | `history.css` | `history.js`, `data.js`, `auth.js` | 스캔 기록 | A형 헤더 + 하단네비 |
| `guide.html` | `guide.css` | `guide.js` | 재질별 안전 가이드 | A형 헤더 + 하단네비 |
| `mypage.html` | `mypage.css` | `mypage.js`, `data.js`, `auth.js` | 마이페이지 | A형 헤더 + 하단네비 |
| `profile-edit.html` | `profile-edit.css` | `profile-edit.js`, `auth.js` | 프로필 수정 | B형 헤더 (백버튼) |
| `notification-center.html` | `notification.css` | `notification.js`, `data.js`, `auth.js` | 알림 센터 | B형 헤더 (백버튼) |
| `notification-settings.html` | `notification-settings.css` | `notification-settings.js` | 알림 설정 | B형 헤더 (백버튼) |
| `account-deletion.html` | `account-deletion.css` | `account-deletion.js`, `auth.js` | 회원 탈퇴 | B형 헤더 (백버튼) |
| `support.html` | `support.css` | `support.js` | 고객 지원/FAQ | B형 헤더 (백버튼) |

### 2-2. 공통 리소스

| 파일 | 설명 |
|------|------|
| `css/common.css` | 전역 디자인 시스템 (CSS 변수, 리셋, 공통 컴포넌트) |
| `js/auth.js` | Auth 객체 (localStorage 기반 인증 관리) + showToast 함수 |
| `js/data.js` | WaveData 객체 (localStorage 기반 데이터 CRUD) + 샘플 데이터 |
| `js/modal.js` | 모달 유틸리티 |
| `img/logo.jpg` | WaveScan 로고 이미지 |

### 2-3. 디자인 시스템 분석

- **폰트**: Google Fonts `Gowun Dodum` + Font Awesome 6.5.1 아이콘
- **컬러 팔레트**: CSS 변수 기반 (--color-primary: #008A93 티알/민트 계열)
- **레이아웃**: 모바일 앱 형태 max-width 430px 중앙 정렬
- **헤더**: fixed, height 60px, 글래스모피즘
- **하단 네비**: fixed, height 64px, 5개 탭 (홈/기록/스캔/가이드/마이페이지)
- **애니메이션**: fadeInUp, anim-delay-* 클래스

### 2-4. 헤더 유형 분류

| 유형 | 적용 페이지 | 구성 |
|------|-------------|------|
| **A형 - 브랜드 헤더** | 홈, 기록, 스캔결과, 가이드, 마이페이지 | 로고+타이틀 왼쪽, 알림버튼 오른쪽 |
| **B형 - 백버튼 헤더** | 프로필수정, 알림센터, 알림설정, 회원탈퇴, 고객지원 | 뒤로가기 왼쪽, 타이틀 중앙, 우측 액션 |
| **없음** | 스플래시, 로그인, 회원가입, 스캔(전체화면) | - |

---

## 3. React 프로젝트 구조

```
D:\조윤상\WaveScan-main
├── org/                          ← 원본 파일 보존 위치
│   ├── *.html
│   ├── css/
│   ├── js/
│   └── img/
├── public/
│   └── img/
│       └── logo.jpg              ← 원본 로고 복사
├── src/
│   ├── components/               ← 공통 컴포넌트
│   │   ├── layout/
│   │   │   ├── AppHeader.tsx     ← A형 헤더 (브랜드)
│   │   │   ├── BackHeader.tsx    ← B형 헤더 (백버튼)
│   │   │   └── BottomNav.tsx     ← 하단 네비게이션
│   │   ├── common/
│   │   │   ├── Toast.tsx         ← 토스트 알림
│   │   │   ├── Badge.tsx         ← 상태 배지 (safe/caution/danger)
│   │   │   ├── EmptyState.tsx    ← 빈 상태 표시
│   │   │   ├── ToggleSwitch.tsx  ← 토글 스위치
│   │   │   ├── ListItem.tsx      ← 리스트 아이템
│   │   │   └── Button.tsx        ← 버튼 컴포넌트
│   │   └── scan/
│   │       └── ScanCard.tsx      ← 스캔 결과 카드
│   ├── pages/                    ← 페이지 컴포넌트
│   │   ├── SplashPage.tsx
│   │   ├── AuthStartPage.tsx
│   │   ├── LoginPage.tsx
│   │   ├── SignupPage.tsx
│   │   ├── HomePage.tsx
│   │   ├── ScanPage.tsx
│   │   ├── ScanResultPage.tsx
│   │   ├── HistoryPage.tsx
│   │   ├── GuidePage.tsx
│   │   ├── MyPage.tsx
│   │   ├── ProfileEditPage.tsx
│   │   ├── NotificationCenterPage.tsx
│   │   ├── NotificationSettingsPage.tsx
│   │   ├── AccountDeletionPage.tsx
│   │   └── SupportPage.tsx
│   ├── hooks/
│   │   ├── useAuth.ts            ← 인증 커스텀 훅
│   │   ├── useToast.ts           ← 토스트 커스텀 훅
│   │   └── useData.ts            ← 데이터 커스텀 훅
│   ├── utils/
│   │   ├── auth.ts               ← Auth 유틸 (auth.js → TS 변환)
│   │   └── data.ts               ← WaveData 유틸 (data.js → TS 변환)
│   ├── types/
│   │   └── index.ts              ← 공통 타입 정의 (ScanData, User 등)
│   ├── styles/
│   │   └── GlobalStyle.ts        ← styled-components GlobalStyle (common.css 변환)
│   ├── App.tsx                   ← 라우터 설정
│   └── main.tsx                  ← 앱 진입점
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## 4. 라우팅 설계

| 경로 | 페이지 컴포넌트 | 설명 |
|------|----------------|------|
| `/` | `SplashPage` | 스플래시 → 자동 이동 |
| `/auth` | `AuthStartPage` | 시작 인증 화면 |
| `/login` | `LoginPage` | 로그인 |
| `/signup` | `SignupPage` | 회원가입 |
| `/home` | `HomePage` | 홈 |
| `/scan` | `ScanPage` | 카메라 스캔 |
| `/scan-result` | `ScanResultPage` | 스캔 결과 (`?id=` 쿼리 파라미터) |
| `/history` | `HistoryPage` | 스캔 기록 |
| `/guide` | `GuidePage` | 안전 가이드 (`#앵커` 탭 지원) |
| `/mypage` | `MyPage` | 마이페이지 |
| `/profile-edit` | `ProfileEditPage` | 프로필 수정 |
| `/notifications` | `NotificationCenterPage` | 알림 센터 |
| `/notification-settings` | `NotificationSettingsPage` | 알림 설정 |
| `/account-deletion` | `AccountDeletionPage` | 회원 탈퇴 |
| `/support` | `SupportPage` | 고객 지원/FAQ |

---

## 5. 타입 정의 (`src/types/index.ts`)

```typescript
// 사용자 타입
export interface User {
  isLoggedIn: boolean;
  provider: 'google' | 'guest';
  name: string;
  email: string;
  avatar: string;
  loginTime?: string;
}

// 스캔 체크리스트 아이템
export interface ChecklistItem {
  name: string;
  status: 'pass' | 'fail' | 'warn';
  text: string;
}

// 스캔 데이터
export interface ScanData {
  id: string;
  date: string;
  timestamp: number;
  material: string;
  materialCode: string;
  grade: 'safe' | 'caution' | 'danger';
  gradeTitle: string;
  gradeDesc: string;
  maxTemp: number;
  bpaStatus: string;
  confidence: number;
  imageUrl: string;
  checklist: ChecklistItem[];
  aiComment: string;
}

// 통계 타입
export interface Stats {
  total: number;
  safe: number;
  caution: number;
  danger: number;
  accuracy: string;
}

// 알림 타입
export interface Notification {
  id: string;
  type: 'scan' | 'guide' | 'notice' | 'danger';
  title: string;
  desc: string;
  time: string;
  read: boolean;
}
```

---

## 6. styled-components 전략

### 6-1. GlobalStyle (common.css → GlobalStyle.ts)

`common.css`의 모든 내용을 `createGlobalStyle`로 변환:
- CSS 변수 (`:root`) 그대로 유지
- 리셋 스타일
- `.app-header`, `.bottom-nav`, `.main-content` 등 공통 클래스
- `.btn`, `.card`, `.badge-*` 등 공통 UI
- `@keyframes fadeInUp`, `.anim-*` 애니메이션
- Google Fonts import, Font Awesome CDN

### 6-2. 페이지별 Styled-components

각 페이지/컴포넌트 파일 내부에 `styled-components`로 스타일 정의:

```typescript
// 예시: HomePage.tsx
import styled from 'styled-components';

const HeroSection = styled.div`
  padding: 20px 16px 0;
  /* home.css의 .hero-section 내용 그대로 */
`;

const HeroCard = styled.div`
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
  border-radius: var(--radius-lg);
`;
```

### 6-3. 공통 컴포넌트 Props 설계

```typescript
// Badge 컴포넌트
interface BadgeProps {
  variant: 'safe' | 'caution' | 'danger';
}

// Button 컴포넌트
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger';
  size?: 'default' | 'sm';
  fullWidth?: boolean;
}

// AppHeader Props
interface AppHeaderProps {
  title: string;
  showNotif?: boolean;
  notifDot?: boolean;
}

// BackHeader Props
interface BackHeaderProps {
  title: string;
  backTo?: string;
  rightAction?: React.ReactNode;
}
```

---

## 7. 커스텀 훅 설계

### `useAuth.ts`
```typescript
// auth.js → React 훅 변환
export const useAuth = () => {
  const [user, setUser] = useState<User | null>(null);
  // getUser, isLoggedIn, loginWithGoogle, setGuestMode, logout, updateProfile
  return { user, isLoggedIn, login, logout };
};
```

### `useToast.ts`
```typescript
// showToast 함수 → React Context/훅 변환
export const useToast = () => {
  const showToast = (message: string, duration?: number) => { ... };
  return { showToast };
};
```

### `useData.ts`
```typescript
// WaveData → React 훅 변환
export const useData = () => {
  const getScans = (): ScanData[] => { ... };
  const saveScan = (scan: ScanData) => { ... };
  const deleteScan = (id: string) => { ... };
  const getStats = (): Stats => { ... };
  return { getScans, saveScan, deleteScan, getStats };
};
```

---

## 8. 단계별 작업 순서

### Phase 0 — 사전 준비

- [ ] **0-1.** 기존 소스를 `org/` 폴더로 복사하여 원본 보존
- [ ] **0-2.** Vite + React + TypeScript 프로젝트 초기화
  ```bash
  npx create-vite@latest . --template react-ts
  ```
- [ ] **0-3.** 필요 패키지 설치
  ```bash
  npm install react-router-dom styled-components
  npm install -D @types/styled-components @types/react-router-dom
  ```
- [ ] **0-4.** `public/img/logo.jpg` 원본 로고 복사
- [ ] **0-5.** `index.html` Font Awesome CDN 및 Google Fonts 링크 추가

---

### Phase 1 — 글로벌 스타일 & 타입 설정

- [ ] **1-1.** `src/types/index.ts` — 공통 타입 정의 작성
- [ ] **1-2.** `src/styles/GlobalStyle.ts` — `common.css` 전체를 `createGlobalStyle`로 변환
- [ ] **1-3.** `src/utils/auth.ts` — `auth.js` TypeScript 변환 (Auth 객체 + showToast)
- [ ] **1-4.** `src/utils/data.ts` — `data.js` TypeScript 변환 (SAMPLE_SCANS + WaveData)
- [ ] **1-5.** `src/main.tsx` — GlobalStyle 적용, StrictMode 설정
- [ ] **1-6.** `src/App.tsx` — React Router 설정 (전체 라우팅 등록)

---

### Phase 2 — 공통 컴포넌트 개발

- [ ] **2-1.** `src/hooks/useAuth.ts` — 인증 훅
- [ ] **2-2.** `src/hooks/useToast.ts` — 토스트 훅
- [ ] **2-3.** `src/hooks/useData.ts` — 데이터 훅
- [ ] **2-4.** `src/components/common/Toast.tsx` — 토스트 컴포넌트
- [ ] **2-5.** `src/components/common/Badge.tsx` — 상태 배지
- [ ] **2-6.** `src/components/common/EmptyState.tsx` — 빈 상태
- [ ] **2-7.** `src/components/common/ToggleSwitch.tsx` — 토글 스위치
- [ ] **2-8.** `src/components/common/ListItem.tsx` — 리스트 아이템
- [ ] **2-9.** `src/components/common/Button.tsx` — 버튼
- [ ] **2-10.** `src/components/layout/AppHeader.tsx` — A형 헤더 (브랜드+알림)
- [ ] **2-11.** `src/components/layout/BackHeader.tsx` — B형 헤더 (백버튼+중앙타이틀)
- [ ] **2-12.** `src/components/layout/BottomNav.tsx` — 하단 네비게이션 (현재 경로로 active 자동 처리)
- [ ] **2-13.** `src/components/scan/ScanCard.tsx` — 스캔 카드

---

### Phase 3 — 페이지 개발

#### 3-A. 인증/온보딩 페이지

- [ ] **3-1.** `SplashPage.tsx` — `splash.html` + `splash.css` + `splash.js` 변환
  - 로딩 바 애니메이션 → useEffect + setTimeout
  - 완료 후 `/auth` 또는 `/home` 라우팅
- [ ] **3-2.** `AuthStartPage.tsx` — `auth-start.html` + `auth-start.css` + `auth-start.js` 변환
  - Google 로그인, 게스트 로그인 버튼
- [ ] **3-3.** `LoginPage.tsx` — `login.html` 변환 (AuthStart와 거의 동일 구조)
- [ ] **3-4.** `SignupPage.tsx` — `signup.html` 변환

#### 3-B. 메인 탭 페이지 (헤더 + 하단 네비)

- [ ] **3-5.** `HomePage.tsx` — `index.html` + `home.css` + `home.js` 변환
  - 게스트 배너, Hero CTA, 통계 그리드, 최근 스캔 캐러셀, 가이드 미리보기
  - 파일 업로드 인풋 처리 (useRef)
- [ ] **3-6.** `HistoryPage.tsx` — `history.html` + `history.css` + `history.js` 변환
  - 통계 바, 검색 필드, 필터 칩, 기록 리스트
- [ ] **3-7.** `GuidePage.tsx` — `guide.html` + `guide.css` + `guide.js` 변환
  - 탭 네비 (PP/세라믹/유리/멜라민/스테인리스/알루미늄)
  - URL 해시(`#앵커`) → `useLocation` 처리
- [ ] **3-8.** `MyPage.tsx` — `mypage.html` + `mypage.css` + `mypage.js` 변환
  - 로그인 뷰 / 게스트 뷰 분기
  - 프로필 통계, 메뉴 리스트

#### 3-C. 스캔 관련 페이지

- [ ] **3-9.** `ScanPage.tsx` — `scan.html` + `scan.css` + `scan.js` 변환
  - 카메라 뷰파인더, 셔터 버튼, 앨범 선택
  - 전체화면 (헤더/푸터 없음)
- [ ] **3-10.** `ScanResultPage.tsx` — `scan-result.html` + `scan-result.css` + `scan-result.js` 변환
  - `useSearchParams`로 `?id=` 파라미터 처리
  - 등급 카드, 재질 분석, 안전 파라미터, AI 의견, 액션 버튼

#### 3-D. 서브 페이지 (백버튼 헤더)

- [ ] **3-11.** `ProfileEditPage.tsx` — `profile-edit.html` 변환
- [ ] **3-12.** `NotificationCenterPage.tsx` — `notification-center.html` 변환
- [ ] **3-13.** `NotificationSettingsPage.tsx` — `notification-settings.html` 변환
- [ ] **3-14.** `AccountDeletionPage.tsx` — `account-deletion.html` 변환
- [ ] **3-15.** `SupportPage.tsx` — `support.html` 변환

---

### Phase 4 — 검증 및 마무리

- [ ] **4-1.** 전체 라우팅 이동 테스트 (각 페이지 간 링크 확인)
- [ ] **4-2.** 레이아웃 비교 검증 (원본 HTML vs React 렌더링 픽셀 단위 일치 확인)
- [ ] **4-3.** 폰트/글씨 크기 일치 확인 (Gowun Dodum, Font Awesome)
- [ ] **4-4.** 반응형 확인 (max-width 430px 모바일 기준)
- [ ] **4-5.** localStorage 인증/데이터 흐름 테스트
- [ ] **4-6.** 빌드 테스트 (`npm run build`)

---

## 9. 주의 사항

### 9-1. CSS 변환 원칙

> **원본 퍼블리싱과 100% 동일한 레이아웃/글씨크기/폰트를 유지해야 합니다.**

- `common.css`의 CSS 변수(`:root`)는 `GlobalStyle.ts`에 그대로 유지
- 각 페이지 CSS 파일의 모든 속성값을 `styled-components`로 1:1 변환
- 픽셀 값, 폰트 크기, 컬러 값을 임의로 변경하지 않음
- CSS 클래스명을 styled-component로 변환 시 원본 클래스에 해당하는 모든 속성 포함

### 9-2. 헤더/푸터 처리

- `AppHeader`와 `BackHeader`는 별도 컴포넌트로 분리
- `BottomNav`는 현재 `useLocation().pathname`으로 active 탭 자동 처리
- 스플래시, 로그인, 회원가입 계열 페이지에는 헤더/푸터 미포함
- 스캔 페이지(`/scan`)는 전체화면 — 헤더/푸터 없음

### 9-3. 인증 흐름

- 원본과 동일하게 `localStorage` 기반 유지
- `Auth` 유틸을 TypeScript로 변환, React Context 또는 커스텀 훅으로 래핑
- 스플래시 완료 후: 기존 로그인 정보 있으면 `/home`, 없으면 `/auth`

### 9-4. 쿼리 파라미터

- `scan-result.html?id=` → `useSearchParams()` 훅으로 처리
- `guide.html#앵커` → `useLocation().hash`로 탭 선택 처리

### 9-5. 이미지/에셋

- `img/logo.jpg` → `public/img/logo.jpg`로 복사 (Vite public 디렉토리)
- 이미지 경로: `/img/logo.jpg` (절대 경로)

### 9-6. 외부 CDN

`index.html`에 직접 링크 유지:
```html
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gowun+Dodum&display=swap">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
```

---

## 10. 설치 패키지 목록

```json
{
  "dependencies": {
    "react": "^18.x",
    "react-dom": "^18.x",
    "react-router-dom": "^6.x",
    "styled-components": "^6.x"
  },
  "devDependencies": {
    "@types/react": "^18.x",
    "@types/react-dom": "^18.x",
    "@types/styled-components": "^5.x",
    "@vitejs/plugin-react": "^4.x",
    "typescript": "^5.x",
    "vite": "^5.x"
  }
}
```

---

## 11. 작업 완료 기준

- [ ] 모든 15개 페이지가 React 컴포넌트로 구현됨
- [ ] 원본 HTML과 레이아웃, 폰트, 글씨 크기, 색상이 동일
- [ ] React Router로 모든 페이지 간 이동 정상 동작
- [ ] styled-components로 스타일 완전 이관 (원본 CSS 파일 미사용)
- [ ] TypeScript 타입 오류 없이 빌드 성공 (`npm run build`)
- [ ] LocalStorage 인증/데이터 로직 정상 동작
- [ ] 원본 소스는 `org/` 폴더에 보존됨

---

*작성일: 2026-09-02*
*작성 기준: WaveScan-main 원본 소스 전체 분석 결과*
